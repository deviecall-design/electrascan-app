import React from "react";
import ScreenPlaceholder from "./ScreenPlaceholder";

export default function EstimateScreen() {
  return (
    <ScreenPlaceholder
      title="Estimate"
      phase="Phase 3"
      description="Desktop estimate editor: line items grouped by category, per-tenant rate library applied, margin/GST controls, lock & finalise action. Generates the branded PDF attached to DocuSign envelopes."
    />
  );
}
