import React from "react";
import ScreenPlaceholder from "./ScreenPlaceholder";

export default function PricingScheduleScreen() {
  return (
    <ScreenPlaceholder
      title="Pricing Schedule"
      phase="Phase 2"
      description="Per-point rate library. Browse the catalogue (Power Points, Lighting, Switching, Switchboard, Cabling, Data & Comms) and override default rates with tenant-specific custom prices. Rates flow automatically into new estimates."
    />
  );
}
