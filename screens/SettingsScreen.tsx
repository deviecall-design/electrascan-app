import React from "react";
import ScreenPlaceholder from "./ScreenPlaceholder";

export default function SettingsScreen() {
  return (
    <ScreenPlaceholder
      title="Settings"
      phase="Phase 6"
      description="Company profile (name, ABN, licence, address, primary email), logo upload, default margin, quote validity, DocuSign connection, team access. Powers every branded PDF and email the tenant sends."
    />
  );
}
