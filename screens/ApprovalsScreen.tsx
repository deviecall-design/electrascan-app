import React from "react";
import ScreenPlaceholder from "./ScreenPlaceholder";

export default function ApprovalsScreen() {
  return (
    <ScreenPlaceholder
      title="Approvals"
      phase="Phase 5"
      description="DocuSign-powered multi-party signing workflow. Envelope message composer, signatory tracking (Builder → Architect → Vendor), live status via DocuSign MCP, envelope preview, signing timeline, and Supabase audit trail."
    />
  );
}
