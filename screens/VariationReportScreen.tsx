import React from "react";
import ScreenPlaceholder from "./ScreenPlaceholder";

export default function VariationReportScreen() {
  return (
    <ScreenPlaceholder
      title="Variation Report"
      phase="Phase 4"
      description="Desktop reskin of the variation report built for mobile: base estimate vs new scan, categorised line-item diff (added / removed / increased / decreased), risk flags, one-click branded PDF export with auto-incremented estimate number."
    />
  );
}
