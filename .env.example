# Anthropic
VITE_ANTHROPIC_API_KEY=

# Supabase (browser client uses these via services/supabaseClient.ts)
SUPABASE_URL=https://zxeznkuodpseijkvjwxa.supabase.co
SUPABASE_ANON_KEY=

# DocuSign — required for /api/approvals/send-envelope. Without these the
# Approvals UI shows a "DocuSign not configured" message instead of the
# Send for Approval button.
DOCUSIGN_INTEGRATION_KEY=
DOCUSIGN_ACCOUNT_ID=
DOCUSIGN_BASE_URI=https://demo.docusign.net
DOCUSIGN_ACCESS_TOKEN=

# Vercel KV (used by /api/sessions/handoff for QR session continuity)
KV_REST_API_URL=
KV_REST_API_TOKEN=
