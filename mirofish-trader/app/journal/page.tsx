import { Panel } from "@/components/Panel";

export default function JournalPage() {
  return (
    <Panel title="Trade Journal">
      <p className="text-mute text-sm">Filter by asset, theme, outcome — wiring up once trades table has rows.</p>
    </Panel>
  );
}
